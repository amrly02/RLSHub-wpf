<Window x:Class="RLSHub.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RLSHub"
        MinWidth="900" MinHeight="600"
        Width="1200" Height="750"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource ShellBackgroundBrush}">
    <Grid>
        <!-- Animated background: soft ellipses + falling BeamNG-style logos (triangle with circles at vertices) -->
        <Canvas IsHitTestVisible="False" ClipToBounds="False">
            <!-- Soft gradient orbs (subtle color theming) -->
            <Ellipse Width="420" Height="420" Fill="{StaticResource AccentBrush}" Opacity="0.12" Canvas.Left="-140" Canvas.Top="-160"/>
            <Ellipse Width="360" Height="360" Fill="{StaticResource AccentBrush}" Opacity="0.08" Canvas.Left="820" Canvas.Top="-120"/>
            <Ellipse Width="520" Height="520" Fill="#FFFFFF" Opacity="0.04" Canvas.Left="520" Canvas.Top="520"/>

            <!-- Falling BeamNG-style symbols (triangle + 3 circles, theme colors) -->
            <Canvas x:Name="FallingLogosCanvas" ClipToBounds="False">
                <!-- Symbol: equilateral triangle with circles at each vertex; repeated with different positions and animation -->
                <Viewbox Width="50" Height="50" Canvas.Left="80" Canvas.Top="-40" RenderTransformOrigin="0.5,0.5">
                    <Viewbox.RenderTransform><RotateTransform Angle="15"/></Viewbox.RenderTransform>
                    <Canvas Width="24" Height="24">
                        <Path Stroke="{StaticResource BgLogoAccentBrush}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 12,2 L 20.2,17 L 3.8,17 Z" Fill="Transparent"/>
                        <Ellipse Fill="{StaticResource BgLogoAccentBrush}" Width="5" Height="5" Canvas.Left="9.5" Canvas.Top="0"/>
                        <Ellipse Fill="{StaticResource BgLogoAccentBrush}" Width="5" Height="5" Canvas.Left="17.7" Canvas.Top="14.5"/>
                        <Ellipse Fill="{StaticResource BgLogoAccentBrush}" Width="8" Height="8" Canvas.Left="0" Canvas.Top="13"/>
                    </Canvas>
                </Viewbox>
                <Viewbox Width="46" Height="46" Canvas.Left="380" Canvas.Top="-60">
                    <Viewbox.RenderTransform><RotateTransform Angle="22"/></Viewbox.RenderTransform>
                    <Canvas Width="24" Height="24">
                        <Path Stroke="{StaticResource BgLogoPinkBrush}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 12,2 L 20.2,17 L 3.8,17 Z" Fill="Transparent"/>
                        <Ellipse Fill="{StaticResource BgLogoPinkBrush}" Width="5" Height="5" Canvas.Left="9.5" Canvas.Top="0"/>
                        <Ellipse Fill="{StaticResource BgLogoPinkBrush}" Width="5" Height="5" Canvas.Left="17.7" Canvas.Top="14.5"/>
                        <Ellipse Fill="{StaticResource BgLogoPinkBrush}" Width="8" Height="8" Canvas.Left="0" Canvas.Top="13"/>
                    </Canvas>
                </Viewbox>
                <Viewbox Width="44" Height="44" Canvas.Left="1050" Canvas.Top="-90">
                    <Viewbox.RenderTransform><RotateTransform Angle="-5"/></Viewbox.RenderTransform>
                    <Canvas Width="24" Height="24">
                        <Path Stroke="{StaticResource BgLogoCyanBrush}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 12,2 L 20.2,17 L 3.8,17 Z" Fill="Transparent"/>
                        <Ellipse Fill="{StaticResource BgLogoCyanBrush}" Width="5" Height="5" Canvas.Left="9.5" Canvas.Top="0"/>
                        <Ellipse Fill="{StaticResource BgLogoCyanBrush}" Width="5" Height="5" Canvas.Left="17.7" Canvas.Top="14.5"/>
                        <Ellipse Fill="{StaticResource BgLogoCyanBrush}" Width="8" Height="8" Canvas.Left="0" Canvas.Top="13"/>
                    </Canvas>
                </Viewbox>

                <!-- RLS team logos (mix of both, fewer) -->
                <Image Width="46" Height="46" Canvas.Left="280" Canvas.Top="-20" Tag="rls-logo-fire" Opacity="0.35" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform><RotateTransform Angle="-6"/></Image.RenderTransform>
                </Image>
                <Image Width="42" Height="42" Canvas.Left="620" Canvas.Top="-60" Tag="rls-logo-redline" Opacity="0.35" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform><RotateTransform Angle="10"/></Image.RenderTransform>
                </Image>
                <Image Width="40" Height="40" Canvas.Left="920" Canvas.Top="-50" Tag="rls-logo-fire" Opacity="0.35" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform><RotateTransform Angle="-8"/></Image.RenderTransform>
                </Image>
            </Canvas>
        </Canvas>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Top nav bar with sliding underline indicator -->
            <Border x:Name="NavBarBorder" Grid.Row="0" Background="#0A0A0A" BorderBrush="{StaticResource SurfaceBorderBrush}" BorderThickness="0,0,0,1" Padding="24,10,24,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="2"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <!-- Header (title + subtitle): aligned so spacing above/below is equal -->
                    <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,32,10">
                        <TextBlock x:Name="HeaderTitle" Style="{StaticResource SectionHeaderTextStyle}" Text="Dashboard"/>
                        <TextBlock x:Name="HeaderSubtitle" Style="{StaticResource HeroSubtitleTextStyle}" Text="RLS Career Overhaul command center." Margin="0,2,0,0"/>
                    </StackPanel>
                    <!-- Nav items: grid so each tab's hover extends to bottom of bar (indicator inside) -->
                    <Grid Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" HorizontalAlignment="Right">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ToggleButton x:Name="NavDashboard" Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Style="{StaticResource NavToggleStyle}" Tag="dashboard" IsChecked="True" Click="Nav_Click" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Dashboard" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton x:Name="NavCarSwap" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Style="{StaticResource NavToggleStyle}" Tag="carswap" Click="Nav_Click" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="CarSwap" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton x:Name="NavUpdates" Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Style="{StaticResource NavToggleStyle}" Tag="updates" Click="Nav_Click" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE895;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Updates" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton x:Name="NavSettings" Grid.Row="0" Grid.Column="3" Grid.RowSpan="2" Style="{StaticResource NavToggleStyle}" Tag="settings" Click="Nav_Click" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Settings" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>
                        <ToggleButton x:Name="NavAbout" Grid.Row="0" Grid.Column="4" Grid.RowSpan="2" Style="{StaticResource NavToggleStyle}" Tag="about" Click="Nav_Click" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE897;" FontFamily="Segoe MDL2 Assets" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock Text="About" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>
                        <!-- Sliding accent underline (on top so it draws over the hover area) -->
                        <Border x:Name="NavIndicatorStrip" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" Height="2" HorizontalAlignment="Stretch">
                            <Border x:Name="NavIndicator" Height="2" HorizontalAlignment="Left" Background="{StaticResource AccentBrush}" VerticalAlignment="Stretch"/>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- Content area with entrance transition (slide + fade like WinUI 3) -->
            <Grid x:Name="ContentHost" Grid.Row="1" ClipToBounds="True">
                <ContentControl x:Name="ContentFrame" Background="Transparent"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
