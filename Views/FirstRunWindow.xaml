<Window x:Class="RLSHub.Wpf.Views.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to RLS Hub"
        Width="480" Height="340"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{StaticResource ShellBackgroundBrush}" CornerRadius="16" BorderBrush="#20FFFFFF" BorderThickness="1" Padding="32"
            Opacity="0">
        <Border.RenderTransform><TranslateTransform Y="20"/></Border.RenderTransform>
        <Border.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4" EasingFunction="{StaticResource EaseOut}"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="20" To="0" Duration="0:0:0.4" EasingFunction="{StaticResource EaseOut}"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Border.Triggers>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Welcome to" FontSize="13" Foreground="{StaticResource TextMutedBrush}"/>
                <TextBlock Text="RLS Hub" FontSize="28" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,2,0,0"/>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock x:Name="BeamNgStatusText" TextWrapping="Wrap" Style="{StaticResource CardSubtitleTextStyle}" Margin="0,0,0,12"/>
                <TextBlock Text="CarSwap connects this app to the in-game marketplace. You can run the bridge from the CarSwap tab anytime." TextWrapping="Wrap" Foreground="{StaticResource HeroControlMutedBrush}" FontSize="13" Margin="0,0,0,16"/>
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SetUpLaterButton" Content="Set up later" Style="{StaticResource GhostButtonStyle}" Click="SetUpLater_Click" Padding="20,10" Margin="0,0,10,0"/>
                <Button x:Name="RunBridgeButton" Content="Run bridge now" Click="RunBridge_Click" Padding="20,10"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
